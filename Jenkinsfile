pipeline {
   agent any

   stages {
      stage('Verify Branch') {
         steps {
            echo "$GIT_BRANCH"
         }
      }
      stage('stage 2') {
         steps {
            echo "$JOB_NAME"
         }
      }      
       
      
   }
}
